{
  "name": "microblog",
  "description": "",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "prepare": "npx lefthook install",
    "web": "npm run dev -w web",
    "api": "npm run dev -w api",
    "format": "npm run format -w web && npm run check -w api"
  },
  "workspaces": [
    "web",
    "api"
  ],
  "devDependencies": {
    "@biomejs/biome": "^2.0.5",
    "lefthook": "^1.11.14"
  }
}
