{
  "name": "microblog",
  "description": "",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build:web": "npm run build --prefix web",
    "web": "npm run dev --prefix web",
    "api": "npm run dev --prefix api",
    "format": "npm run format --prefix web && npm run format --prefix api"
  }
}
